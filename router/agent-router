#!/usr/bin/env python3
"""Resolve the portable CLI beside this script, including when symlinked on PATH."""
from pathlib import Path
import runpy
import sys

directory = Path(__file__).resolve().parent
sys.path.insert(0, str(directory))
runpy.run_path(str(directory / "cli.py"), run_name="__main__")
