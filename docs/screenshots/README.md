# Installation screenshots

Captured from the installed ORCA 1.4.206 macOS application with Agent Router
0.1.0 on 2026-09-22, using ORCA's computer-use screenshot capture. These are
unmodified PNG captures of the application window.

| File | Screen |
| --- | --- |
| `01-plugins-settings.png` | Settings → Plugins, Plugin system enabled |
| `02-development.png` | Development expanded with the local repository registered |
| `03-installed.png` | Installed tab, Agent Router awaiting review |
| `04-permissions.png` | Agent Router's access review and shortcut conflict |

The repository was already registered when these were captured. Its displayed
path is an example; readers must use their own checkout path. The permission
dialog shows a repeat review rather than a fresh installation.

When refreshing these images, capture the same screens, check for credentials
or unrelated private content, and update the version in the root README.
